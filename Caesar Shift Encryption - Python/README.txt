Description: This code is a simple piece of python framework that encrypts a normal plain text input, and 
outputs the cipher code that has been implimented the certain ammount of times of [inputKey, see file].

Parameters: The input needs to be in a alphabet format either uppercase or lowercase but they will both be
processed through lowercase.

Charlie Walker